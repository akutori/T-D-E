```uml
@startuml
state 検索結果ページ{


    state 検索結果:do/検索結果の表示
    state 検索結果:entry/DBアクセス
    state "商品詳細" as goodsdetail <<sdlreceive>>
    


    [*] -> ヘッダー
    
    検索結果 --> goodsdetail :商品がクリックされたとき
    検索結果 --> 検索結果 :検索情報が入力\nor\n検索ボタンが押された場合
    

    state ヘッダー<<headder>>{
        state fork_header <<fork>>
        state ifinput <<choice>>
        state "タブ" as tab <<sdlreceive>>
        state "カート" as cart <<sdlreceive>>
        state "プロフィール" as profile <<sdlreceive>>
        state "メインページ" as mainpage <<sdlreceive>>

        tab -right-> ifinput 
        ifinput -up-> cart :カートをクリック
        ifinput -> profile :プロフィールアイコン\nor\nユーザー名をクリック
        ifinput -> mainpage :カートをクリック
        ifinput --> fork_header :ジャンル\n・\nハードをクリック

        mainpage --> fork_header :検索情報が入力\nor\n検索ボタンが押された場合
        検索結果 --> mainpage :サイトロゴをクリック
        fork_header -> 検索結果
    }

    skinparam stateBorderColor<<headder>> #d3a3ff
    state ヘッダー #d3a3ff

}

@enduml
```